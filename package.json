{
  "name": "wod-translator",
  "private": true,
  "packageManager": "pnpm@11.25.0",
  "version": "0.0.1",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "pretest": "pnpm --filter @wod-translator/shared build",
    "test": "pnpm -r test",
    "pretypecheck": "pnpm --filter @wod-translator/shared build",
    "typecheck": "pnpm -r typecheck"
  }
}
